{
  "name": "typeorm",
  "version": "0.2.34",
  "description": "typeorm shim so I don't have to use paths on CRA",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "class-transformer": "^0.4.0"
  }
}
